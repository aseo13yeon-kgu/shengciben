# 生词本 · 중국어 단어장 기획서

## 1. 개요

- **이름**: 生词本 (중국어 단어장)
- **형태**: `vocab.html` 단일 파일 웹페이지
- **목적**: 한자 / 병음 / 한국어 뜻을 직접 입력해 나만의 단어장을 만들고 관리
- **대상**: 경기대학교 중어중문전공 학습용 개인 프로젝트

## 2. 핵심 기능

| 기능 | 설명 |
|---|---|
| 단어 입력 | 한자 / 병음 / 한국어 뜻 세 칸 입력 |
| 추가 버튼 | 버튼 클릭 또는 Enter로 목록에 단어 추가 |
| 브라우저 저장 | `localStorage`에 저장되어 새로고침해도 목록 유지 |
| 삭제 버튼 | 각 줄 오른쪽 ✕ 버튼으로 개별 삭제 |
| 검색창 | 한자 또는 한국어 뜻에 포함된 텍스트로 실시간 필터링 |
| 외운 단어 체크 | 각 줄 왼쪽 체크 버튼으로 암기 여부 표시 (취소선 처리) |
| 체크 숨기기 버튼 | 외운 단어만 목록에서 숨기고, 다시 눌러 전체 보기로 복귀 |

## 3. 디자인 방향 (단정한 톤)

- **상단 헤더**: 진한 남색(`#1F2A44`) 띠, 흰 글자로 제목 표시
- **본문 배경**: 아주 옅은 회색(`#FAFAF9`)
- **목록**: 하나의 박스 안에 줄마다 구분선을 두고, 짝수 줄은 연한 회색(`#F3F4F7`)으로 번갈아 표시(줄무늬)
- **한자**: 26px, 굵게, 진한 잉크색(`#24252A`)
- **병음**: 12px, 연회색(`#9497A0`)으로 작게
- **한국어 뜻**: 14px, 기본 잉크색
- **포인트 컬러**: 남색으로 통일
  - 추가 버튼: 남색 배경 + 흰 글자
  - 체크 버튼(on): 남색 배경
  - 숨기기 버튼(활성 상태): 남색 배경
- **글꼴**
  - 한자: `Noto Serif SC`
  - 한국어/UI: `Noto Sans KR`

## 4. 데이터 저장 방식 & 공유 범위

- 데이터는 서버가 아닌 **브라우저 `localStorage`**에 저장됨 (단어장별로 `zh-vocab-notebooks` 키에 저장, 과거 단일 단어장(`zh-vocab-list`) 데이터는 첫 실행 시 자동으로 이전됨)
- 저장된 단어는 **저장한 브라우저 + 기기 안에서만** 유지됨
- 다른 사람이 같은 `vocab.html` 파일을 자신의 브라우저에서 열면 빈 단어장으로 보이며, 내 단어는 보이지 않음
- 온라인(GitHub Pages 등)에 배포해도 방문자마다 독립적으로 저장되며 서로 공유되지 않음

## 5. 기술 구성

- 순수 HTML / CSS / Vanilla JavaScript (프레임워크 없음)
- Google Fonts CDN: `Noto Serif SC`, `Noto Sans KR`
- 단어 데이터 구조 예시:
  ```json
  { "id": "고유값", "hanzi": "谢谢", "pinyin": "xièxie", "meaning": "고맙습니다", "checked": false }
  ```
- 검색: 한자 / 한국어 뜻 문자열에 대한 대소문자 무시 부분일치(`includes`) 검색
- 체크 숨기기: 화면 렌더링 시 `hideChecked` 플래그로 필터링 (데이터 자체는 삭제되지 않음)

## 6. 확장 기능 (구현 완료)

- [x] 단어 수정(편집) 기능 — 각 줄 ✎ 버튼으로 한자/병음/뜻을 인라인으로 수정
- [x] 여러 단어장(카테고리/폴더) 관리 — 상단 셀렉트로 단어장 전환, +/✎/🗑 버튼으로 추가·이름변경·삭제
- [x] 데이터 내보내기 / 가져오기 (JSON 파일) — 목록 하단 "내보내기/가져오기" 버튼
- [x] 외운 단어 통계 표시 — 목록 상단에 `N개 · 암기 M개 (P%)` 형태로 표시
